<header class="hero" style="background-image: url('{{ Image(module.background_image).src('hero_large') }}')">
    <div class="hero__inner">
        <div class="hero__eyebrow">{{ module.eyebrow }}</div>
        <h2 class="hero__heading">{{ module.heading }}</h2>
    </div>
</header>
